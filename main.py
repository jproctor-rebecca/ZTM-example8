# expressions vs statements

iq = 100
user_age = iq / 5
  # iq / 5 is an expression because it produces a value
  # user_age = iq / 5 is a statement because it is an entire line of code that performs an action

